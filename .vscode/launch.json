{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/build/tasklib_test",
            "args": [],
            "preLaunchTask": "Build - Debug",
            "cwd": "${workspaceRoot}"
        }
    ]
}
