{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/build/tasklib",
            "args": [],
            "preLaunchTask": "Build - Debug",
            "cwd": "${workspaceRoot}"
        }
    ]
}
